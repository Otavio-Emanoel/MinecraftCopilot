plugins {
	// Permite ao Gradle baixar automaticamente a versão de JDK requisitada pelo toolchain
	id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
}

rootProject.name = 'MinecraftCopilot'
